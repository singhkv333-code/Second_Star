node_modules
# npm's interrupted-install leftovers; see tsconfig.json's exclude.
node_modules.rollback
.next
.next.prev
.vercel
.env*
!.env.example

# tsc --build's incremental fingerprint. It is regenerated by every build and
# was tracked, so the working tree read dirty on a clean checkout forever.
tsconfig.tsbuildinfo
