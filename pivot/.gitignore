# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/

# Environment
.env
*.env.local

# Database
*.db
*.sqlite

# IDE
.vscode/settings.json
.idea/
*.swp

# Logs
*.log

# Telegram session files (created by scripts/auth_telegram.py).
# Contain encrypted auth keys — must never be committed. The
# trailing `.*` catches the journal + any diagnostic copies
# (`.session.diag`, `.session.bak`, etc.).
*.session
*.session.*
*.session-journal
telegram.session

# OS
.DS_Store
Thumbs.db

# logo override generator checkpoint (scripts/build_logo_overrides.py)
backend/market/logo_overrides_progress.json
