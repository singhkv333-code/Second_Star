# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 incompatible with bcrypt >= 4.1
python-multipart==0.0.12

# Cache
redis==5.1.1

# HTTP client (for Sarvam AI, external APIs)
httpx==0.27.2

# Broker
kiteconnect==5.0.1

# TOTP (for Kite auto-login)
pyotp==2.9.0

# Market data (free, no API key needed)
yfinance==0.2.66
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
# pandas_ta is unmaintained on PyPI for Python 3.11/3.12; use the maintained fork
pandas_ta_classic>=0.4

# F&O Greeks/IV (P0). py_vollib_vectorized = Jäckel rational IV solver
# (vectorized, numba-JIT) for chain screening; py_vollib (scalar) is the
# unit-test oracle for the owned Black-76 module. Both are OPTIONAL at
# runtime — backend/market/greeks/iv.py falls back to an owned
# Newton-Raphson+Brent solver when the numba toolchain isn't available.
py_vollib>=1.0.1
py_vollib_vectorized>=0.1.1

# Scheduler (SIP + strategy cron jobs)
apscheduler==3.10.4

# Timezones (IST for market-hours checks)
pytz==2024.2

# JSON Schema validation (workflow step config validation, draft 2020-12)
jsonschema==4.23.0

# Logging + observability
structlog==24.4.0
sentry-sdk[fastapi]==2.18.0

# Encryption (Fernet for Kite tokens at rest)
cryptography==43.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Utilities
python-dotenv==1.0.1

# News & Event Trigger — Phase 3 article-body extraction
trafilatura==1.12.2

# News & Event Trigger — Phase 7 Telegram MTProto channel reader
# (Tier-A push transport per docs/news_events_phase7_*.md). Pin to a
# 1.x release — Telethon 2.x has breaking API changes.
telethon>=1.36,<2.0

# Runtime deps the app imports but were previously absent here (the venv
# failed to boot without asyncpg, and compare_performance crashed without
# `ta`). Versions verified working against the live chat path.
asyncpg==0.31.0          # backend/routers/expr_backtest.py
openai>=1.40             # LLM client (Azure + OpenAI fallback)
ta==0.11.0               # technical-analysis indicators (compare_performance)
